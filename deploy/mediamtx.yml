###############################################
# MediaMTX configuration
# Ingest-only: RTMP in, nothing served publicly.
# Auth and hooks are wired in ticket #7.
###############################################

logLevel: info

rtsp: no
rtmp: yes
rtmpAddress: :1935
hls: no
webrtc: no
srt: no
moq: no

paths:
  all:
